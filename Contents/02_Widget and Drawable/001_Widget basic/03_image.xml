<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageButton
        android:id="@+id/imageButton"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="40dp"
        android:layout_marginLeft="40dp"
        android:background="@drawable/ok_btn"
        android:contentDescription="ok button"
        >
    </ImageButton> <!-- 
    □ 이미지 버튼은 이미지를 버튼처럼 만들고 싶을 때 사용한다.
      □ 하지만 이미지 버튼은 일반적인 버튼과 다르게 눌린 상태와 눌리지 않은 상태가 표시되지 않는다.
    -->

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="160dp"
        android:layout_marginLeft="160dp"
        android:background="@drawable/person"
        android:contentDescription="person button"
        ></ImageView>

</LinearLayout>

<!--
□ android:src 또는 app:srcCompat="@drawable/이미지 파일명" : 원본 이미지를 설정한다. 이미지뷰는 이미지가 지정되지 않으면 이미지뷰의 크기를 확인할 수 없으므로
  반드시 설정해야 한다.
□ maxWidth, maxHeight : 이미지가 표시되는 최대 폭, 높이
□ tint : 이미지뷰에 보이는 이미지 색상을 설정할 수 있다.
□ scaleType : 이미지뷰의 크기에 맞게 원본 이미지의 크기를 자동으로 늘리거나 줄여서 보여줄 때 사용한다.
□ 이미지 파일은 PNG 포맷을 권장한다.
-->
