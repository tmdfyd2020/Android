<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/menu_refresh"
        android:title="새로고침"
        android:icon="@drawable/menu_refresh"
        app:showAsAction="always" /> <!--
        ○ item : 하나의 메뉴에 대한 정보를 담고 있음
        ○ icon : 아이콘으로 표시
        ○ showAsAction="always" : 이 메뉴를 항상 보이도록 설정
          ○ always                 | 항상 액션바에 아이템을 추가하여 표시한다.
          ○ never                  | 액션바에 아이템을 추가하여 표시하지 않는다.
          ○ ifRoom                 | 액션바에 여유 공간이 있을 때만 아이템을 표시한다.
          ○ withText               | title 속성으로 설정된 제목을 같이 표시한다.
          ○ collapseActionView     | 아이템에 설정한 뷰(actionViewLayout으로 설정한 뷰)의 아이콘만 표시한다.

        ♡ android: -> 기본 API에 포함된 속성
        ♡ app: -> 프로젝트에 들어 있는 속성
          ○ 외부 라이브러리에서 제공되는 속성도 app:로 참조한다.
        -->

    <item android:id="@+id/menu_settings"
        android:title="설정"
        android:icon="@drawable/menu_settings"
        app:showAsAction="always" />

    <item android:id="@+id/menu_search"
        android:title="검색"
        android:icon="@drawable/menu_search"
        android:orderInCategory="102"
        app:showAsAction="always|withText"
        app:actionLayout="@layout/search_layout" />

</menu>
